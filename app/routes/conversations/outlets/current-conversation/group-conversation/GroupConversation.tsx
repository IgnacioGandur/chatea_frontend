import styles from "./GroupConversation.module.css";

export default function GroupConversation() {
    return (
        <section className={styles.groupConversation}>
            <h1>Group conversation</h1>
        </section>
    );
}
